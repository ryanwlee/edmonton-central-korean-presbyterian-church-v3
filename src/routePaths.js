// Plain, JSX-free so the Playwright suite can import it directly.
// src/routes.test.js asserts this stays in sync with the real router config.
export const ROUTE_PATHS = [
  "/",
  "/intro",
  "/service",
  "/announcement",
  "/reserve",
  "/serving",
  "/education",
  "/jubo",
  "/admin",
];
