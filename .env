# react-scripts 5.0.1 pulls eslint 8 and eslint-config-react-app into its own
# nested node_modules, so the project root cannot resolve the "react-app" config
# and the build fails before it emits anything. Linting does not affect the
# compiled output, so it is disabled here to keep builds working.
DISABLE_ESLINT_PLUGIN=true
